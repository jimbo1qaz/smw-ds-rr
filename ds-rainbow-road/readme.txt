parse_vol_hex(vol) = return, not put

%pbend delay time note
%vbend time volume
%vib delay frequency amplitude
%trem delay frequency amplitude

%segment = NO IMPLICIT RESET

%passthrough = self-explanatory

%isvol, %notvol
%ispan, %notpan